import { defineConfig } from 'astro/config';

export default defineConfig({
  site: 'https://jin-s13.github.io/awesome-auto-research-hub',
  base: '/awesome-auto-research-hub' || undefined,
  output: 'static',
});
